# Earth command UI verification

The local development app loaded successfully after waiting. It opened its persisted Browser workspace by default, so the Earth dashboard requires selecting the Dashboard control before inspecting the 3D Earth module. The rendered app title is `Bonds Studio — Orbital Workspace` and the browser workspace remains intact after the Earth command-bar code and global location bundle were added.
The dashboard exposes the new Earth command controls as a horizontal menu: Location input, Time select, and Enter submit. Typing `Tokyo` surfaced global-array suggestions including Tokyo and Nishi-Tokyo-shi with coordinates. Selecting `12:00 PM` and clicking Enter applied the command: the profile changed to Tokyo, Japan; the header changed to `Manual command · centered`; the detail panel showed the global city array source, coordinates, `12:00 PM local`, `Centered location`, and the centered-scene label updated to Tokyo with `12:00 PM`. The Earth globe visual also shifted to the selected scene state.
After the refinement build, the app hot-reloaded cleanly. Returning from the persisted Browser workspace to Dashboard restored the Earth module with the Location text input, Time select, and Enter button visible. The default simulated profile resumed normally, and the 24-option time menu remains available alongside LIVE.
