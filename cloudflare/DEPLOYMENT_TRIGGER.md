# Bonds Transceiver deployment

Deployment trigger for the Cloudflare Worker edge runtime.

The deployment workflow uses GitHub Actions secrets `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID`; no Cloudflare credential is stored in this repository.
