# AI Mode verification notes

The `/ai-mode` route renders successfully in the browser with the Bonds Studio shell, guest-isolated session list, model selector populated from the live model catalog, reasoning selector, reusable chat surface, capability panel, and tool activity panel. A guest conversation is created and listed automatically.

A low-risk workspace overview prompt was submitted. The UI remained in a loading state after the request, with no browser console error. Server logs showed the request reaching the development server but did not show a completion or error before the observation window ended. The next debugging step is to inspect the tRPC/network request response and, if necessary, add explicit request timeout/error handling around the server-side LLM call.

The request completed successfully after approximately 20 seconds with HTTP 200. The response was generated by `gpt-5-mini`, persisted into the conversation, and reloaded through `ai.getConversation`; the model returned a workspace summary for the empty guest workspace. The apparent loading state was simply the model latency rather than a client error.
