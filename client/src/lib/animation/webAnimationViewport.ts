export type DeviceClass="phone"|"tablet"|"desktop"|"wide";
export type WindowMode="fullscreen"|"windowed"|"split"|"embedded";
export type AnimationViewportPolicy={device:DeviceClass;mode:WindowMode;width:number;height:number;dpr:number;reducedMotion:boolean;touch:boolean;renderScale:number;timelineDensity:"compact"|"standard"|"expanded";maxPreviewFps:number;autoLayout:"mobile"|"tablet"|"desktop"|"cinema"};
export function detectViewportPolicy():AnimationViewportPolicy{const w=window.innerWidth,h=window.innerHeight;const touch="ontouchstart"in window||navigator.maxTouchPoints>0;const device=w<640?"phone":w<1024?"tablet":w<1800?"desktop":"wide";const reduced=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const fullscreen=!!document.fullscreenElement;const mode=fullscreen?"fullscreen":w<900?"windowed":w/h>2.2?"split":"windowed";return{device,mode,width:w,height:h,dpr:window.devicePixelRatio||1,reducedMotion:reduced,touch,renderScale:device==="phone"?.7:device==="tablet"?.8:device==="desktop"?.9:1,timelineDensity:device==="phone"?"compact":device==="wide"?"expanded":"standard",maxPreviewFps:reduced?30:device==="phone"?30:60,autoLayout:device==="phone"?"mobile":device==="tablet"?"tablet":mode==="split"?"cinema":"desktop"}};
export function observeViewport(onChange:(policy:AnimationViewportPolicy)=>void){const emit=()=>onChange(detectViewportPolicy());const ro=new ResizeObserver(emit);ro.observe(document.documentElement);window.addEventListener("resize",emit,{passive:true});document.addEventListener("fullscreenchange",emit);const mq=window.matchMedia("(prefers-reduced-motion: reduce)");mq.addEventListener?.("change",emit);emit();return()=>{ro.disconnect();window.removeEventListener("resize",emit);document.removeEventListener("fullscreenchange",emit);mq.removeEventListener?.("change",emit)}}
